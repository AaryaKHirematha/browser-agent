{
  "name": "browser-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome MV3 browser layer + agent for get_state()-driven web automation.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
